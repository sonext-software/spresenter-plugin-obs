{
  "name": "spresenter-plugin-obs",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Plugin OBS Studio para o Spresenter — nós de automação via obs-websocket v5.",
  "scripts": {
    "build": "vite build --config vite.code.config.ts",
    "package": "npm run build && node scripts/package.mjs",
    "dev": "vite build --watch --config vite.code.config.ts"
  },
  "dependencies": {
    "@spresenter/plugin-sdk": "file:sdk/spresenter-plugin-sdk.tgz"
  },
  "devDependencies": {
    "adm-zip": "^0.5.17",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
